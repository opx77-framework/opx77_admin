---@meta

--- How many vehicles this resource spawned still exist, after forgetting the ones the host no
--- longer knows. For the status readout.
---@return integer
function OpxAdmin.Server.SpawnedCount() end
